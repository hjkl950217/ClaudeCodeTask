# 项目进度追踪

> 本文件用于会话中断后恢复任务。每完成一个阶段就更新此文件。

## 项目目标

做一个工具，解决「本地有很多含 CLAUDE.md 的任务文件夹，每次要手动 cd 过去再启动 claude code」的痛点。
目标是在一个交互式列表里选择任务文件夹，自动进入并启动 Claude Code 会话。

## 当前阶段

**阶段 13 进行中：第十三轮优化（增量缓存提速）已完成，待用户复测**

第十三轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 83 | 还有没有可能更快？会话记录次数可以用大概值（如文件行数） | **样本分析证伪「行数近似 UserMsgs」**：130 文件 2063 条真实用户消息 / 156467 行，整体比例仅 0.013（约 77 行才 1 条，jsonl 多为 assistant/tool 行），比例跨度 200 倍（min 0.001 ~ max 0.208），阈值边界区文件（精确 6-15 条）行数跨度 32~13097；任何行数阈值或「行数÷K」映射判断 ≥10 的误判率 79-92%——几乎所有 <10 条会话被当成 ≥10，列表被虚假会话淹没。**保留精确 userMsgs，放弃行数近似** |
| 84 | （随 83）基于缓存策略但要融合：先判断会话数量再用对应策略，参数拐点用测试设定 | **融合增量缓存**：文件数 ≥ 拐点 **20**（实测 N≤20 全扫平缓 152ms、N≥20 线性涨至 662ms；小文件担忧不存在——实测最小 13.4KB、0 个 <10KB）且缓存存在时，按 mtime+size 判定未变文件直接复用上次精确扫描结果、只重扫变化文件；文件 < 拐点走原全量方案且**不读写缓存**（少量会话时原本方案足够快，且测试 mock 树天然走全量零污染）。缓存存 `~/.cct/cache.json`（{version, files:{path:{mt,sz,raw}}}，raw=精确 string[6]），读/写失败静默回退全量 |

第十三轮验证（2026-09-01）：热启动（缓存命中无变化）285-367ms（第十二轮基线 1061ms，快约 65-73%）；变更 3 文件增量 344ms；全量 Pester 13 文件 136/136 通过（新增 Cache.Tests 4 例：≥拐点首次全量后写缓存、未变复用结果一致、追加消息触发重扫、< 拐点不读写缓存）。

第十二轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 82 | 输入 cct 后扫描历史会话较慢 | **剖析定位**：Get-CctTasks 全程 ~2.9s，C# 串行逐文件扫描 298MB/133 jsonl 占 2338ms（87%）为主瓶颈，Test-Path 过滤 241ms 次之。修复：①新增 `CctScannerV4::ScanAll`——Parallel.For 并发读盘+扫描（并行度=min(文件数, CPU 核)，文件间无共享状态，单文件异常返回空记录不中断），PowerShell 一次调用取全部结果再转换，免逐文件互操作；②阶段 3 过滤 `Test-Path` → `Directory.Exists`（纯 .NET 目录判定，免 PowerShell 提供程序开销）。类名 V3→V4 换名（ScanAll 签名变更，Add-Type 类型缓存见决策 61）。结果：ScanAll 801ms（串行 2338ms）、全程 1061ms（~2.9s→1.1s，2.7x），ScanAll 与 ScanFile 结果一致性 0/133 不一致 |

第十二轮验证（2026-09-01）：ScanAll 并行 801ms（串行基线 2338ms）；Get-CctTasks 全程 1061ms（旧版基线 ~2900ms）；133 文件 ScanAll/ScanFile 结果逐项一致；全量 Pester 12 文件 132/132 通过无回归。真实 cct 启动手感待长空确认。

第十一轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 80 | 终端窗口变化停止后必须滚动/方向键按一下才显示 | **根因**：resize 检测到尺寸变化只 `ESC[2J` 清屏 + 置空 CctLastRows，随后继续轮询等按键；下一帧重绘要等外层 while 循环经 ReadKey 返回后才发生 → 停止后屏幕空白待按键。修复：检测到 resize 置 `$resized` + break，跳过 ReadKey 直接 `continue` 回外层循环顶部立即用新尺寸重绘 |
| 81 | 移动选中时，卡片所在行的其他未选中卡片闪烁 | **根因**：Write-CctFrame 行 diff 对变化行先 `ESC[K` 清整行再重写——选中移动时整行内容变化（该行所有卡片被一起清空重写）→ 同行其他卡闪。修复：改覆盖式写入（直接写新内容，每行自带完整 ANSI 颜色状态，卡片行可见宽恒定），仅当新行可见宽 < 旧行时 `ESC[K` 清尾、`$l2=null`（行删除）时 `ESC[K` 清行 |

第十一轮验证（2026-09-01）：探针 5 场景 PASS（纯颜色变化行无 `ESC[K` / 缩短行写后清尾 / 删行清行 / 无变化零输出 / 变长只覆盖无清行）；全量 Pester 12 文件 132/132 通过无回归。resize 立即重绘与真实拖拽手感待长空复测。

第九轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 70 | 会话恢复失败（公益站签到）：选中后 "No conversation found to continue"，L1(-r)=1 → L2(-c)=1 → L3(prompt)=1 全失败；再运行 cct 选其他项正常启动 | **根因（探针实锤）：jsonl 物理存储目录 = 会话启动目录（cwd-first）的编码目录**。旧版 CctScannerV2 只记 lastCwd（末现 cwd），按它分组会造出「幻影 Folder」——指向无编码目录的路径（公益站签到/js/claude-code-zh-cn 三个），选中后 `-c` 在错误路径找不到会话。实测 4 个公益站签到 jsonl（6fcc5d0a/78aa0f88/c02464d8/e264dcb4）都存在 `E-----PersonalAITaskCenter-------sub2api`（=管理_sub2api）：cwd-first 全为管理_sub2api、cwd-last 为公益站签到 → 幻影 Folder（33 个 Folder 恰好 3 个无效，21 个 Session 全部有效）。修复：扫描器版本化 V3 返回 firstCwd（启动/存储目录，分组键）+lastCwd（末现目录，resume 目标）；按 firstCwd 归组 → 幻影天然消失、有效会话挂回父目录；Session 项 Path 仍用 lastCwd（决策 9 先 Set-Location 的目标）。类名 V2→V3 换名（Add-Type 类型缓存，见决策 61） |
| 71 | 仍有一些无效会话被识别出来 | 随 70 一并根治：幻影 Folder 3→0，21/21 Session 的 sessionId 文件均实测存在。本轮未加额外 sessionId 校验（探针已证全有效），避免无谓开销 |
| 72 | 选中卡片后的启动文字末尾旋转符号变「...」（原为 \|/-\ 动画） | 反馈澄清（用户纠正：指选中后的启动文字，非列表项显示）。修复：`Invoke-CctClaude` 改由 `Invoke-WithSpinner`（C# CctSpinner 青色 \|/-\ 旋转 + AutoStopMs 6000 兜底）包裹 Start-Process；移除静态 `Write-Host "… …"`。决策 55 「spinner 后台线程与 claude 输出并发撕裂」根因已被第四轮 Start-Process 保 TTY 根治（claude 接管真实 TUI 覆盖 spinner），残留风险仅冷启动前 1-2s；重定向/测试环境 IsOutputRedirected=true 时 Invoke-WithSpinner 直跑不启 spinner |
| 73 | 「↓ 还有 33 项」滚动后数字不变（bug） | 根因：hidden 提示行在滚动区外按 `Tasks.Count - visibleCards` 计算，滚动帧重算但计数语义混乱（visibleCards 只反映当前可见槽）。因反馈 5 要求删除该行 → 整行删除即根治 |
| 74 | 底部空行太多（卡片固定预留一行 + 卡片间留一行 + 还有 XX 项） | **精简布局**：去块尾空行（卡片帧槽 6→5 行，卡间不留空行）、删「↓ 还有 X 项」隐藏提示行。帧高恒 = MaxRows×5+3 ≤ 屏高（第九轮公式 `(h-3)/5`；固定栏 3 行：搜索+空+帮助），帮助行恒在帧末、行号稳定（行 diff 不重绘）。删死变量 `$visibleCards` |

第九轮验证（2026-09-01）：全量 Pester 12 文件 130/130 通过。数据层 StartCwd 归组测试（DataAggregate 新增 2 例：中途切换会话归 StartCwd 组 + 幻影 sub 文件夹消失；DataExtract 新增 StartCwd 取第一条）；真实数据回归 5/5（文件夹全存在且非 worktree、Session 项文件全存在——幻影 Folder 消失）；Launcher 降级链 14/14（spinner 包裹 Start-Process 不破坏 -r/-c/prompt 三级链）；Selector 帧高 13（MaxRows*5+3）与滚动固定栏 26/26。

第十轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 75 | 拖动终端大小后显示不刷新：底部残留完整/半截旧卡片（帮助行下方出现卡片残影） | **根因（探针实锤）**：resize 检测触发时 `$script:CctLastRows = @()` 置空，而 `Write-CctFrame` 行 diff 遍历 `Max(旧帧行数,新帧行数)`——置空后旧帧行数=0，旧帧比新帧多的行永不重绘（`ESC[K` 清行逻辑够不到）→ 物理屏残留旧卡片。修复：resize 改为「先真清屏 `ESC[2J` + 光标回家，再置空 CctLastRows → 下一帧全量重绘」，旧内容彻底清除 |
| 76 | 帮助行距离窗口底部还有很多行（不足一个卡片高度 5 行）；拖动窗口变大后底部显示不动 | 上半部分同 75 根因（resize 后不刷新）。**帮助行贴底**：`New-CctFrame` 签名 `MaxRows`→`WindowHeight`，内部 `MaxRows=floor((h-3)/5)`，余数行 `(h-3)%5` 填充到帮助行上方，帧高恒=WindowHeight → 帮助行恒在窗口末行，窗口拖高/拖宽时全量重绘跟随；小窗口兜底 `MaxRows≥0`（h<8 无卡片仅固定栏，不溢出滚屏） |
| 77 | 无效会话问题消失 | 第九轮按 firstCwd 归组修复确认有效，无需改动 |
| 78 | 选中样式整卡反色太重，希望小一点、匹配卡片边框线条；卡片间距可再小 | 选中整卡（含边框 5 行）由 `ESC[7m` 反色改为 `ESC[96m` 亮青文字（AskUserQuestion 确认「整卡亮青文字」）；卡间距 2 空格→1 空格（`$gap=1`，cardW 公式同步，列数阈值 36 不变） |
| 79 | 搜索条占位文案 | 改「直接输入即可按关键词实时过滤，支持中文」 |

第十轮验证（2026-09-01）：全量 Pester 12 文件 132/132 通过（新增 2 例：WindowHeight 余数行填充 + 帮助行贴底、卡间距 1 空格；选中断言 7m→96m；Selector 28 例全过）。冒烟渲染 W=120/H=15/16：帧高恒=窗高、帮助行贴底、填充行在帮助行上方、卡间距 1 空格、Session 前缀括号正常。resize 清屏路径由代码逻辑保证（ESC[2J+全量重绘），真实终端拖拽复测待长空确认。

第八轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 65 | 会话项括号显示最后一级目录名（CC汉化（CC_汉化））疑似不对 | 决策 48 规则本身如此（「标题（会话所在目录名）」），但标题与目录名几乎相同时括号是噪音。**归一化去重**：去空白/`_`/`-`/`.`、忽略大小写后相同 → 省略括号。`CC汉化`目录 CC_汉化 → 只显示 `CC汉化`；`账号调优（管理_sub2api）` 保留。新函数 `Get-CctSessionLabel`（Display.ps1） |
| 66 | 滚动后末尾提示行错乱（「提示 提示」残影/显示别的内容） | **根因**：MaxRows = (h-5)/3 按每块 3 行预算，但决策 47 加路径第二行后每块实际 4 行，帧高超出屏高 9 行，终端滚屏后绝对定位行号把超出行推入滚动区 → 整屏错位。修复：**固定栏位布局**——帧高恒 = MaxRows×6+4 ≤ 屏高（边框卡每块 6 行：5 内容+1 空行；固定栏 4 行：搜索+空+隐藏提示+帮助），搜索/帮助行行号恒定，行 diff 跳过不重绘 = 固定栏效果，终端永不滚屏。隐藏提示行恒在位（无隐藏时空串） |
| 67 | 搜索条提示文字 | 改「直接输入即可按关键词实时过滤」 |
| 68 | 卡片加边框，选中反色含边框整卡 | box-drawing 边框（┌─┐│└┘ 五行卡，Windows Terminal 渲染 1 列与宽度计算一致），卡间 2 空格；选中 5 段（含上下边框）全部 `ESC[7m` 反色。单卡最小宽 34→36（内容区不变） |
| 69 | 时间未右对齐、路径超宽压到时间列 | 时间列 `Pad-DisplayLeft` 左补空格右对齐（占满 timeW=10），名称段 nameW 单独预算；路径两行独立预算 padW 不再与时间列重叠 |

第八轮验证（2026-09-01）：单元测试 129/129；E2E（真实 conhost 子窗口 + C# Timer 注入 Down×12/Esc + ReadConsoleOutput 抓帧 + PrintWindow PNG）12/12；PNG 像素级复核：边框渲染完整、选中含边框整卡反色（白底黑字）、固定栏滚动前后一致、滚动后第二列卡片反色位置正确。

> E2E 探针经验（第八轮，勿再踩）：
> 1. 主进程（Claude Code 宿主）stdout 是管道，FreeConsole+AttachConsole 后 GetStdHandle 拿回旧管道句柄 → ReadConsoleOutput err=6。必须「子窗口内部自驱」（C# Timer 静态回调 + GetStdHandle 天然有效）
> 2. conhost 下 `[Console]::OutputEncoding = UTF8` 会让子进程 UI 直接收到空列表（注入链路被干扰）——探针脚本禁用此行，产品代码不受影响（用户 Windows Terminal 正常）
> 3. ReadConsoleOutput 的 DllImport 必须声明 `CharSet=CharSet.Unicode`，否则 CJK/制表符按 ANSI 读出全是 NUL/FFFD（文本帧乱码）；但 conhost OEM CP 下 CJK 写入本身仍会变 U+FFFD——文本帧只能验 ASCII 结构，CJK 视觉效果靠 PNG 像素分析复核
> 4. .NET 10 的 System.Drawing Add-Type 引用链：System.Drawing → System.Drawing.Common → System.Private.Windows.GdiPlus → System.Private.Windows.Core（一环缺一环报 CS1069/CS0012），最后还要补 System.Threading（显式 ReferencedAssemblies 覆盖默认引用）
> 5. Claude Code 网关（cc-switch）不支持图片 content 块——主会话 Read PNG 会报 400 毒化会话（上个会话因此报废）。改用 PNG 像素分析（LockBits + 颜色统计 + ASCII art 渲染）在文本通道完成视觉复核

第六轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 63 | 进 UI 后方向键变 `[C[C[D` 进搜索框、无法筛选、ESC/Ctrl+C 均无法退出 | **根因（探针矩阵实锤）**：stdin 残留 ENABLE_VIRTUAL_TERMINAL_INPUT(0x0200) 时 `[Console]::ReadKey` 把方向键拆成 ESC/'['/'C' 三个普通字符（Key=None）——'[' 'C' 'D' 经 `$ch -ge 0x20` 判定进 query（污染源），ESC 字符被过滤（退出失灵根因），Ctrl+C 的 char(3) 也进不了 Escape/C 判定。cct 自身不设置该模式（mode_steps 探针证明 KeyAvailable/WindowWidth/TreatControlCAsInput/alt-screen 均不影响），但用户终端可能带着进来（PSReadLine.dll 含 ENABLE_VIRTUAL_TERMINAL_INPUT 符号，是残留来源候选）。修复：`Enter-CctInputMode` 进入选择器时保存 mode 并清 VT_INPUT 位、退出恢复（防御性设计，不依赖谁设置的模式） |
| 64 | （随 63 发现）注入实验的方法论 | VT 开启时 WriteConsoleInput 注入的 VK 事件**在写入时即被转换为字符流**——先设 VT 再注入的实验形态会夸大问题；真实场景用户按键发生在 UI 已清模式之后。E2E 必须用「先进 UI 清模式再注入」时序（trace 实验形态） |

第五轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 61 | 「没有可显示的任务」（Import-Module -Force 重导也无效） | **根因：Add-Type 类型缓存**。内联 C# 编译进进程 AppDomain，`Import-Module -Force` 只重载 PS 函数不重编译 C#；旧签名 CctScanner（双参 ScanFile）留在第三轮测试时开的常驻终端里，新代码调单参重载抛 MethodException 被 catch 吃掉 → 所有会话变空记录 → 全过滤 → 空列表。测试进程每次新建所以从未复现（教训：**dot-source 模块加载测试必须在「旧类型在场」的进程里跑**）。修复：类名版本化 CctScanner → CctScannerV2，新旧共存，常驻终端立即可用；验证：旧类型在场时扫出 56 项 |
| 62 | （随 61 发现）提示打印两次 | `Write-Host $msg` + `return $msg` 返回值被宿主再打一次。修复：提示只走 Write-Host，返回值改空 |

第四轮决策（2026-09-01）：

| # | 反馈 | 决策 |
|---|---|---|
| 56 | L1/L2 报 No deferred tool marker、L3 退出码 -1，用户怀疑会话识别逻辑错误 | **探针矩阵实锤真正根因**：旧形态 `$null = Invoke-WithSpinner { Invoke-Expression }` 中 PowerShell 一旦捕获子进程输出就给 claude 挂 stdout 管道 → claude（Node）检测非 TTY → 进 headless print 模式 → 无 prompt 启动报此错。**三轮以来 headless 化悬案的根因就是启动方式本身**（决策 55 里「未稳定复现的触发条件」即此——测试窗口当时部分走 Start-Process 所以间歇复现）。修复：`Invoke-CctClaude` 改 Start-Process -NoNewWindow -RedirectStandardError -Wait -PassThru 直启（探针 S 系列验证：stdout 保持 TTY + ExitCode Int32 本尊 + stderr 分流，函数嵌套/上层赋值捕获下全部稳定）。会话识别本身没选错：b8c4b9b3 就是「账号调优」组 msgs≥10 中最新的且尾部 tool_use 已闭合 |
| 57 | 参考 cc-switch 会话识别 | 调研吸收两点：①`<local-command-caveat>`/`<command-name>` 伪用户消息不计入 UserMsgs（系统注入非真实输入）；②标题行几乎总在文件尾部（27/27 样本实测） |
| 58 | （随 56 发现）识别失真 | **ScanFile 去早停**：旧版第 10 条用户消息早停 → 长会话 LastTimestamp 停在早停点（b8c4b9b3 显示 08-25 实际活动到 08-31）→ 排序/resume 目标选取失真。全量扫描 304MB 语料实测 3.26s 可接受（早停版 0.7s，正确性优先） |
| 59 | （随 56 发现）L3 prompt 引号形态 | Windows argv 规则：单引号不剥壳，claude 会收到字面 `'继续'`。L3 兜底 prompt 改双引号内联 `` `"继续`" `` |
| 60 | （随 56 验证）Ctrl+C 中断语义 | console 级 Ctrl+C 广播全 console 进程：claude 收到退出、cct 的 pwsh 同步被打断回到提示符（静默、无降级、无报错）——天然满足「启动期可中断」，无需特殊处理 |

第三轮决策（2026-08-31）：

| # | 反馈 | 决策 |
|---|---|---|
| 50 | 选中后显示错乱 + 「账号调优」进不去 | 根因（claude.exe 二进制源码级确认）：CC 2.1.237 无 prompt 交互启动在 stdin 判定为空输入流且无豁免（挂起工具/SessionStart hook 上下文）时报 "No deferred tool marker found"——**与路径检测、会话好坏无关**，错误文案里 deferred marker/tail-scan 全是误导（距尾 6 行的会话同样报错）。显示错乱 = spinner 后台线程与 claude Error 输出竞争控制台行。修复：①stderr 分流到临时文件，spinner 停止后统一回放；②三级降级链 -r → -c → 带 prompt 兜底（实测退出码 0） |
| 51 | spinner 文本带任务名 | `正在启动会话：[账号调优（管理_sub2api）]`（Get-CctSpinnerText，与卡片显示一致） |
| 52 | 降级接续也要 spinner | L2/L3 各带 spinner（接续/兜底启动也要时间） |
| 53 | 全失败输出完整地址 | 诊断块：任务路径 + 会话 ID + 目录存在 + 退出码链 L1/L2/L3 |
| 54 | 启动期监听 Ctrl+C | 实测 PS 默认透传（exit 130）；退出码 130/143 视为用户主动中断**不触发降级**（修隐藏 bug：原实现 Ctrl+C 退出会话会被强制降级重启）|
| 55 | 复测仍见「退出码 长空…0」+ 全链失败 | **双重实锤根因**：①用户终端 claude 以 headless 跑（TUI 未开），L3 回复文本流经管道污染 $exit3 成数组；②PowerShell if(@(文本,0)) 解包判 0 falsy → L3 实际成功（exit 0）误判失败打出污染诊断块。修复（d7e0d8f）：stdout 全程 $null 丢弃 + 退出码 [int] 强转快照 + 静态提示行替代并发 spinner（根治 ESC[2K 与 claude 输出并发撕裂「-史 -」）。注：用户终端 claude headless 化的触发条件多变量实验均未稳定复现（TTY/profile/env/IME 残留/selector/spinner/stdin 判定全部排除），WT 全链路实验呈间歇性 |

> 注：用户终端的必现触发条件未能在测试窗口复现（TTY/profile/纯净环境/并发实例实验全成功；
> 推测与用户终端 conpty stdin 判定边缘情况有关）。修复采用防御性设计：无论触发与否，
> 降级链保证最终能进会话，显示层保证不撕裂。

第二轮决策（2026-08-31）：

| # | 反馈 | 决策 |
|---|---|---|
| 47 | 路径只显示 2 级信息少；计数位置 | 路径 3 级 + 超宽换行（卡片变 3 行，不足补空行）；「共 N 项」紧跟搜索框右侧 |
| 48 | 子任务缩进显示层级弱 | 会话项显示「标题（父目录名）」平铺，如「账号调优（管理_sub2api）」 |
| 49 | 选中坏会话双重报错 | 根因：Temp 探针残留会话（发现 9/22 调研产物）被 -c 接续。修复：cwd 也过排除模式（默认加 AppData\Local\Temp）+ 降级 -c 再失败时明确报错不再重试 |
| — | spinner 残留「史 /」 | 清行宽度按 string.Length 算，中文显示宽 2 列清不干净 → 改 ESC[2K 擦整行 |

第一轮决策（2026-08-31）：

| # | 反馈 | 决策 |
|---|---|---|
| 41 | 扫描期间无输出像卡死 | `Invoke-WithSpinner` 通用旋转指示器（C# 后台线程，winget 风格 `\|/-\` 青色），扫描期间显示「正在扫描会话历史」 |
| 42 | 没进入 claude 会话时终端留在任务目录 | Esc/启动失败/目录失效 → 回退进入 cct 前的原路径；成功进入 claude → 维持决策 22（留在任务目录） |
| 43 | 单列列表堆积不好用 | 界面重构为**卡片式网格**：≤3 列、卡宽按终端宽动态算、每卡 2 行（标题+时间 / 淡色地址）、选中整卡反色、↑↓←→ 四向导航（模仿 CC 点选界面） |
| 44 | 时间列宽度 | 预留 10 显示宽（「59分钟前」= 4 中文×2 + 2 数字 = 10），右对齐 |
| 45 | 终端 resize 无响应 | 主循环 KeyAvailable 轮询（30ms）检测尺寸变化，重算列数/卡宽并全量重绘，selected 保持 |
| 46 | 「搜索没生效」实为反馈缺失 | 过滤逻辑一直在（英文逐键、中文需 IME 提交——发现 10 硬约束）；新 UI 搜索行显式显示当前查询词，占位文案改「实时过滤」；选中后 claude 启动期显示「正在启动会话」spinner（6 秒自动停，避免破坏 claude TUI 界面） |

**阶段 8 完成（11 个 TDD 任务全部实现，87/87 测试通过）**

实现按 `docs/plans/2026-08-27-cct-implementation-plan.md` 执行完毕。
关键实况（与计划的偏差）：
- 性能：PowerShell 逐行处理实测 17 秒（远超设计 3.18 秒预期），
  改用 Add-Type 内联 C# 扫描器后全库 <1 秒（24 倍提速）
- Pester 6.1.0 两个坑：`-BeLessThanOrEqual` 参数集绑定 bug（用 `-BeLessThan N+1` 替代）；
  测试参数 `[ConsoleKey]::Enum` 必须括号包成表达式
- GetNewClosure 变量快照陷阱：按键序列注入改用 Queue 引用类型
- 保底自动项阈值口径统一为 ≥（visio文件解析 10 条压线案例）
- 决策 34 修订（2026-08-27 /goal）：手动命名会话同样受 ≥10 条阈值约束，
  resume 目标取组内 ≥阈值文件里最新的；账号调优从 1 条碎片改指 113 条老文件（c0bb6ac）
- 阶段 9 首个实测 bug：`$script:CctLastRows = $null` 在 `@($null)` 展开边界下报
  「Cannot index into a null array」——初值改 `@()` + 索引 null 兜底修复

待用户手工验证：全屏列表、方向键滚动、中文对齐、Esc 恢复终端、实际启动 claude。

## 阶段清单

- [x] 1. 需求澄清（brainstorming skill，架构性路径）
- [x] 2. 技术能力调研（CLI 参数、会话数据结构、终端交互方案，全部实测）
- [x] 3. 方案对比 —— 实际走向：调研直接排除了不可行方案（会话内 `/xx` 无法切 cwd、
      磁盘扫描会漏 29 个目录、mtime 排序会错排），无需再列备选
- [x] 4. 分段设计评审（通过 4 轮 grilling 盘问完成，31 条决策 + 复审轮决策 32-40）
- [x] 5. 写设计文档
- [x] 6. 用户审阅设计文档（复审轮 + UI 补充后用户指令「好的，你执行吧」）
- [x] 7. 写实现计划（writing-plans skill，11 个 TDD 任务）
- [x] 8. 开发实现（TDD，85/85 通过，提交 ca26086..21a7572 共 11 个功能提交）
- [ ] 9. **验证 + 安装使用** ← 当前：等用户手工验证界面交互后粘贴 Import-Module 到 profile

## 环境补充发现

**Pester 已装 6.1.0**（2026-08-27，`Install-Module Pester -Scope CurrentUser -Force -SkipPublisherCheck`，
冒烟测试通过）。运行测试用 `Import-Module Pester -MinimumVersion 5.0.0` 后 `Invoke-Pester`，
或直接 `pwsh -NoProfile -Command "Invoke-Pester ..."`（注意 Windows 自带 3.4.0 优先级问题，
必须显式 `-MinimumVersion 5`）。Pester 6 的 BeforeAll 必须放在 Describe 直接子级，不能放 It 里。

## 关键技术发现

### 发现 1：会话历史里有原始 cwd（重要）

`~/.claude/projects/` 下的目录名是编码过的（中文/斜杠全变成 `-`，**不可逆**），
例如 `E-----PersonalAITaskCenter----CC----------`。

但目录内的 `<session-id>.jsonl` 文件中，**每条记录都带完整未编码的 `cwd` 字段**：

```json
{
  "type": "attachment",
  "cwd": "E:\\个人\\PersonalAITaskCenter\\工具\\CC插件_管理文件夹会话",
  "timestamp": "2026-08-27T00:54:13.464Z",
  "version": "2.1.237",
  "gitBranch": "main",
  "userType": "external"
}
```

**影响**：
- 「最近活动排序」可直接用 jsonl 文件的 mtime 或 timestamp，无需自建数据库
- 「已有任务列表」可从会话历史反推，等于自带历史数据
- 原需求 3.1（SessionStart 注册）可能整个不必要

注意：jsonl 首几行（`last-prompt` / `mode` / `permission-mode`）没有 cwd，
需要往后扫到第一条带 cwd 的记录（一般是 `attachment` 或 `user` 类型）。

### 发现 2：本地规模

- `~/.claude/projects/` 有 43 个会话项目目录
- 本仓库 `E:\个人\PersonalAITaskCenter` 深度 5 内有 25 个 CLAUDE.md
- 存在嵌套：`0A_参考源码/CLAUDE.md` 和根 `CLAUDE.md` 同时存在（子任务也有 CLAUDE.md）

### 发现 3：必须是 PowerShell 函数，不能是独立 exe（决定性约束）

用户的 `claude-dc` 是 profile 里的**函数**，不是可执行文件：

```powershell
function claude-dc {
    claude --dangerously-skip-permissions -c @args
}
```

位置：`C:\Users\gt\Documents\PowerShell\Microsoft.PowerShell_profile.ps1` 第 176-178 行
（同文件第 172-174 行还有 `claude-d` = 不带 `-c` 的版本）

**关键约束**：子进程无法改变父 shell 的工作目录。要实现「选完自动 cd 到那个目录并在**当前**终端启动 claude」，
入口必须是**在当前 shell 会话内运行的 PowerShell 函数/模块**，通过 `Set-Location` 改目录。
如果做成 .exe 或用 `node xxx.js` 启动，cd 只在子进程里生效，退出后回到原地 —— 需求直接失败。

结论：核心实现语言 = PowerShell。安装方式 = 往 profile 里加一行 `Import-Module`（或直接加函数定义）。

### 发现 4：`claude -c` 在无历史目录会优雅降级

实测：在一个全新空目录下执行 `claude --dangerously-skip-permissions -c --print "hi"`
→ 退出码 0，正常开了个新会话，**没有报错**。

**影响**：默认命令用 `claude-dc`（带 `-c`）对「从未打开过的新任务」也是安全的，
不需要「先判断有没有历史再决定加不加 -c」的分支逻辑。

### 发现 5：本机环境清点

| 项目 | 状态 |
|---|---|
| PowerShell | 7.6.5（pwsh，支持 `&&`、三元运算符） |
| Node | v22.22.2 @ `C:\Program Files\nodejs\node.exe` |
| Python | 3.12 @ `C:\Program Files\Python312\python.exe` |
| **fzf** | **未安装** —— 选择界面必须自己实现，不能依赖外部模糊查找工具 |
| claude 本体 | `C:\Users\gt\.local\bin\claude.exe`（Application，真实 exe） |
| profile | `C:\Users\gt\Documents\PowerShell\Microsoft.PowerShell_profile.ps1`（已存在，含 oh-my-posh + 大量自定义） |
| 命令名占用 | `cct` / `cc` / `ct` / `task` / `tk` / `ccd` / `go` 全部空闲可用 |

profile 里已有 OSC 9;9 + last-cwd 机制（第 195-255 行），用于 Windows 终端新标签继承目录 —— 
如果后续要做「新标签页启动」可复用这套。

### 发现 6：会话标题存在 jsonl 里，可区分手动/自动

两种记录类型，同一 session 有多条时**取最后一条**为最新值：

```json
{"type": "custom-title", "customTitle": "Tailscale网络优化", "sessionId": "1b655a97-..."}
{"type": "ai-title",     "aiTitle": "渐进阅读 Claude Code 会话", "sessionId": "0318b612-..."}
```

- `custom-title` = 用户手动重命名（全库 37 条），基本都是中文，语义准确
- `ai-title` = CC 自动生成（全库 133 条）
- 显示优先级：`customTitle` > `aiTitle` > 无标题

### 发现 7：全量真实数据统计（2026-08-27）

| 指标 | 数值 |
|---|---|
| 会话 jsonl 文件总数 | 442 |
| 唯一 cwd 数 | 60 |
| 含 custom-title 的会话 | 37 |
| 按原始规则生成的列表项 | 93（**其中约 60 项是同名重复，不可用**） |

**噪音来源**：每次 `claude -c` 续接，CC 会新建一个 jsonl 文件并**继承上次的标题**。
所以 `编写在线设计文档` 一个文件夹产生 17 条全叫「在线设计文件」的项，
`管理_sub2api` 产生 13 条（标题在「账号调优 / codex伪装 / 签到」间分布）。
→ 必须按 (cwd, title) 组合去重，同名只留最新。

### 发现 8：必须排除 agent 子会话

会话 ID 存在 `agent-a1e1f34e4859168ca` 这种 `agent-` 前缀的，是子 agent 的 sidechain 会话，
不是用户真实会话，**必须从列表排除**。（记录里另有 `isSidechain` 字段可作交叉判断）

### 发现 9：CLI 参数实测结果（决定性）

`claude --help` 相关参数：

| 参数 | 作用 |
|---|---|
| `-c, --continue` | 继续**当前目录**中最近的对话 |
| `-r, --resume [value]` | 按 session ID 恢复，或打开交互式选择器 |
| `--fork-session` | 恢复时创建新 session ID 而非复用（配合 `-r`/`-c`） |
| `-n, --name <name>` | 设置会话显示名（显示在 `/resume` 选择器和终端标题） |
| `--session-id <uuid>` | 指定会话 ID |

**实测 1**：`claude -c` 在无历史的空目录 → 退出码 0，优雅降级为新会话，不报错。
**实测 2**：`claude -r <sid> --print` 在会话原目录 → 正常恢复，退出码 0。
**实测 3（关键坑）**：在 `C:\...\Temp` 下 resume 一个属于 `E:\...\CC-Switch用量提取代码` 的会话，
问它工作目录，回答是 **`C:\Users\gt\AppData\Local\Temp`**。

> **`--resume` 不会自动回到会话的原始目录，它使用你当前所在的目录。**
> 因此工具的执行顺序必须是：先 `Set-Location <任务目录>`，**再** 调 `claude -r <sid>`。
> 顺序颠倒会导致恢复的会话跑在错误的目录下。

### 发现 10：中文输入法无法实时逐键过滤（硬约束，影响交互设计）

Windows Terminal 的 IME 组字过程（拼音字母、候选窗）**不经过控制台输入缓冲**，
`[Console]::ReadKey()` 读不到。只有 IME **提交候选词**时，最终的中文字符才作为按键事件进入缓冲。

**后果**：
- 英文/数字：可以真正做到「每敲一个字符实时过滤」
- 中文：做不到逐键实时，只能「IME 提交一个词之后」才触发过滤
- 且 IME 提交候选通常按 Enter，会额外产生一个 Enter 事件

**缓解**：让 Enter 的语义正好是「确认当前选择」——IME 提交的字符先被读进输入框，
随后的 Enter 触发确认，顺序恰好符合直觉。要连续输入多个汉字，用数字/空格选候选（不产生 Enter）。

### 发现 11：终端交互技术方案已验证可行

全部在本机 pwsh 7.6.5 实测通过：

| 项目 | 结论 |
|---|---|
| 方向键/Enter/Esc/Backspace 捕获 | `[Console]::ReadKey($true)` 可靠。方向键 KeyChar 是 `\0`，必须用 `.Key` 判断 |
| Ctrl+C 处理 | 需 `[Console]::TreatControlCAsInput = $true`，且只在按键循环期间开启 |
| **中文显示宽度** | .NET **无**内置 East Asian Width；`StringInfo` 无用。必须手动判 Unicode 区间，用 `[System.Text.Rune]` 遍历（正确处理 emoji 代理对）。已产出实测通过的 `Get-DisplayWidth` |
| ANSI 转义 | Windows Terminal 全支持。必须**直接写 `[char]27` 转义串**，不要用 `$PSStyle`（重定向时会被剥离） |
| 防闪烁重绘 | 缓存上一帧行数组，逐行 `-cne` 比较，只重绘变化行。**不要用 `ESC[2J` 全清** |
| 备用屏幕 | `ESC[?1049h` / `ESC[?1049l` 可行，退出后恢复原终端内容。必须在 `finally` 里恢复 |
| 淡色路径 | `ESC[90m`（bright black）= Campbell 配色下 `#767676`。注意选中行若用 `ESC[7m` 反白，淡灰会看不清 |

### 发现 12：JSON 解析性能（决定要不要做缓存）

实测速率对比（pwsh 7.6.5）：

| 方案 | 速率 | 内存风险 |
|---|---|---|
| 逐行 `ConvertFrom-Json` | 21,000 行/秒 | **危险**，约 1.2KB/对象，百万行级会 OOM |
| 逐行 `ConvertFrom-Json -AsHashtable` | 67,000 行/秒 | 中等 |
| **`[System.Text.Json.JsonDocument]` 流式** | **169,000 行/秒** | **低**（6 万行仅增 4MB） |

**结论**：
- 瓶颈在 JSON 解析（CPU），不在 IO（300MB 读盘约 2 秒）
- 必须用 `JsonDocument` 流式逐行解析，只提取需要的字段
- **绝对不要**逐行 `ConvertFrom-Json`
- 过滤是每次按键的高频操作 → 解析一次放内存，按键只做内存过滤，绝不重读文件

产出的验证 demo：`_perf_test/demo_selector.ps1`（含过滤、方向键、淡灰路径、备用屏幕、行 diff 重绘、异常恢复，dry-run 已通过）

### 发现 13：读前 50 + 后 50 行即可拿到全部所需字段（性能问题彻底解决）

全库实测：446 个 jsonl，345 MB，153,443 行。字段位置分布：

| 字段 | 位置 | 只读头尾 50 行的命中率 |
|---|---|---|
| `cwd` 首现 | 99.6%(441/445) 在**前 5 行**，全部在前 26 行 | 445/445 |
| 最后一条 `timestamp` | 410/445 就是最后一行，444/445 距末尾 ≤5 行 | 445/445 |
| `custom-title` | **散布全文**，但最后一条总在末尾附近 | 40/40 |
| `ai-title` | 同上 | 91/91 |

**结论**：读每文件前 50 + 后 50 行 → **100% 命中**，处理量从 15.3 万行降到约 4.4 万行。
不需要做缓存，无状态实现即可，预计耗时 1 秒内。

读尾部要用 seek 从后往前取行，不能 readlines 全量加载（最大单文件 22 MB）。

### 发现 14：agent 文件占一半，用文件名前缀排除即可

- 文件名以 `agent-` 开头：**233 / 446（52%）**
- `isSidechain=true` 的 11,649 条记录**全部**在这 233 个文件内，非 agent 文件内 0 条
- 含 sidechain 的文件集合与 agent 文件集合**100% 重合**

**结论**：用文件名前缀 `agent-` 排除即可，不必解析 `isSidechain`。

另需排除 `journal.jsonl`（workflow 日志，无 cwd 无会话记录）。

### 发现 15：cwd 会话中途会变（13 个文件），必须取最后一条

- 413 个文件只有 1 个 cwd；32 个文件有多个（最多 9 种）
- **13 个文件首条 cwd ≠ 末条 cwd**
- 变化机制：`/add-dir`、用户中途 cd 到子目录、agent worktree 切换
- 实例：`业务模块设计文件` → `子任务/visio解析` → `脚本与流程` → 又变回 `子任务/visio解析`（可双向）

**结论**：cwd 取文件内**最后一条**的值（代表会话结束时所在目录）。
注意这与「发现 13」冲突——cwd 首现在前 5 行，但最后一条 cwd 可能在尾部。头尾都要扫，取尾部优先。

### 发现 16：mtime 不能用于排序（约 1% 会严重错排）

- mtime 是**本地时间**，与 UTC timestamp 恒定差 +28800s（UTC+8）
- 校正后 90.8% 的文件 |差| ≤60s
- 但**5 个文件偏差巨大**（+66h 到 +187h）：这些是被**复制/备份过**的文件
  （如 `ConfigBackup` 备份目录下的，mtime 是复制时间而非活动时间）

**结论**：必须解析最后一条 timestamp 排序，不能用 mtime 近似。
timestamp 本身非严格单调（3667 对相邻倒序），但 97.2% 回退 ≤60s，取最后一条仍可信。

### 发现 17：322/445 个会话文件没有标题（推翻「标题为主」的展示设计）

- 445 个有效文件中，**仅 123 个含 title，322 个无 title**
- 无 title 的文件按 cwd 聚合涉及 51 个唯一目录（`管理_sub2api` 有 53 个无标题会话）
- `custom-title` 去重后只有 **13 种值**：
  `在线设计文件`(686次)、`visio文件解析`(200)、`签到-compressed-ullman`(132)、`账号调优`(98)、
  `签到`(77)、`CC汉化`(72)、`codex伪装`(27)、`统一代码理解`(22)、`Tailscale网络优化`(4)、
  `解决一字一码`(2)、`fix-ui-localization`(1)、`主动汉化未完成`(1)、`解决一字一码-swirling-anchor`(1)

**结论**：显示名**必须以 cwd 目录名为主**，title 只作补充。

### 发现 18：(cwd, title) 去重后仍有 93 项（远超预期，去重规则需加强）

按「同一 cwd 下同名 title 只留最新」去重后**仍是 93 项**——因为**每个 (cwd, title) 组合本来就只对应一个最新文件**，
之前看到的「17 条重复」是因为把 ai-title 和 custom-title 混在一起统计。

真实分布（含 ai-title 后）：
- `工具/管理_sub2api` → **13 项**（账号3244请求头覆写、项目概览、sub2api过程记录、账号调优、codex伪装、清理sub2api目录文件…）
- `个人/PersonalAITaskCenter` → **14 项**（git status概览、源代码仓库路径迁移、Git改动概览、创建浏览器独立代理环境…）
- `CPM相关/FS文件编写` → 4 项，`临时性任务/L站浏览` → 4 项

问题：ai-title 是 CC 给**每次琐碎对话**自动生成的（「git status概览」「查看后台运行任务」「Review pasted text」），
把它们全列出来会让列表极度臃肿且大部分无价值。

**待用户决策**：是否只保留 `custom-title`（你手动命名的 13 种，语义准确）作为会话项，
完全忽略 ai-title？这样列表会从 93 项降到约 60 + 13 = 70 项左右且全部有意义。

### 发现 19：失效路径与无 CLAUDE.md 统计

唯一 cwd：89 个（全部）/ **71 个**（排除 agent 后）

- **38 个目录当前不存在**：
  - worktree 残留 17 个（`.claude/worktrees/agent-xxx`）
  - 普通缺失 21 个（`0A_参考源码/sub2api`、`临时性任务/L站浏览`、`工具/sub2api管理` 等）
- **存在但无 CLAUDE.md：29 个**（`ConfigBackup`、`0A_参考源码/源代码理解/*`、
  **`工具/CC插件_管理文件夹会话`（本项目目录自己）**、`工具/管理_sub2api/scripts`、`E:\公司\AI任务` 等）
- **存在且有 CLAUDE.md：22 个**

**印证了砍掉 CLAUDE.md 扫描的决策是对的**：29 个真实用过的任务目录没有 CLAUDE.md，
若按 CLAUDE.md 过滤会漏掉一大批（包括本项目目录）。

### 发现 20：数据质量良好，无异常

0 字节文件 0 个、损坏行 0 行、cwd 为空 0 条、UNC/网络路径 0 个、控制字符 0 个。
所有 cwd 均为标准 `X:\...` 格式（含中文和空格但转义正常）。

### 发现 21：消息数过滤实测（2026-08-27 补充调研）——聚合模型已被发现 22 证伪，仅早停数据仍有效

> **勘误（发现 22 二次探针）**：本发现开头「必须按逻辑会话聚合，不能按物理文件」的结论
> 是**错的**——基于「`claude -c` 续接拆分文件」的错误假设。实际同标题多文件是独立会话
> （无跨文件关联），**消息数按单文件计**。早停方案（数满 10 即停）不受影响，仍然正确。

**早停方案实测**（用户提出「判断到大于 10 就可以」，已验证可行）：
- 单文件数满 10 条即停止读取该文件
- 真实口径（排除 isMeta 和 tool_result 回传）：全库 3.18 秒，读取量 252MB → 102MB
- user 消息识别：`type=user` 且非 `isMeta` 且 content 为字符串或含 text 块

**过滤后的真实影响**（用户已确认新规则，见决策 32-35）：
- 31 个有效文件夹全部保留（文件夹项永不消失）
- 13 种手动命名会话：8 种存活（目录存在的全部保留，含压线的 visio文件解析 10 条）
- 自动命名会话 >10 条每目录留最新一个作快速入口
- 过滤掉的全是琐碎对话：`git status概览`(4)、`检查项目待提交更改`(4)、`查看后台运行任务`(7) 等

**PowerShell 技术坑（实现时注意）**：`TryGetProperty($name, [ref]$var)` 的 ref 变量
必须先类型化为 `[System.Text.Json.JsonElement]::new()`，传未类型化的 `$null` 会因
无法绑定 `JsonElement&` 重载而每行抛异常被静默跳过（本调研两次踩坑）。

**新增默认排除**：`~/.claude/projects/` 下的 `ConfigBackup` 目录（会话副本，
不排除会对同 cwd 重复计数且 mtime 失真——之前发现 16 的 5 个 mtime 偏差文件就在这里）。
后经探针复核：ConfigBackup 目录里的会话是在备份目录里**真实运行过**的（cwd 指向备份区路径），
不是复制副本——但仍然排除（用户不希望备份目录出现在任务列表）。

### 发现 22：resume 机制探针（2026-08-27，grilling 复审轮）

**小会话实测**（临时目录双消息探针）：
- `claude -c` 续接**不新建文件**，在同一 jsonl 末尾追加，parentUuid 跨运行链无缝衔接
- `claude -r <sessionId>` 恢复也追加到原文件；恢复后能看到全部历史
- `--resume <sessionId>` 方案验证可行：sessionId 稳定、历史完整
- 全库 455 个 jsonl 中 `"type":"summary"` 记录 0 个（当前版本无 summary 机制）

**待二次验证的矛盾**：真实历史存在同标题多文件（「在线设计文件」17 个文件、sessionId 各不同、
跨 08-21~08-27），与小会话实测的「同文件追加」矛盾。怀疑 auto-compact 触发新建文件并继承标题。
多文件场景下 resume 链尾文件（消息最少的最新文件）能否看到早期历史，已派探针验证（进行中）。
**此结论影响**：会话项显示的 sessionId 取哪个文件、以及 `--resume` 后用户能否看到完整历史。

**多文件会话二次探针结果**（「在线设计文件」19 个文件实测，**推翻发现 21 的聚合模型**）：
- 19 个文件之间**无任何跨文件 parentUuid 引用、无共享 uuid、无 summary/compact 记录**——
  它们是**独立会话**，不是同一会话的片段；标题相同是因为 custom-title 被继承
  （源头：用户在 03d7c102 会话里 `/rename`，后续新会话全部带上同名 custom-title）
- resume 链尾文件（02bf4d95，只有 8 行 `/clear` 空壳）→ Claude 明确回答**看不到任何早期历史**
  （"这个会话刚执行了 /clear，之前的对话内容已被清除"）
- 每个文件开头都有 `/clear`（SessionStart:clear hook），推断用户工作流是「新会话先 /clear」，
  所以多文件的成因是「同 cwd 反复新开会话 + 标题继承」，不是 compact 拆分
- `--fork-session` 生成新 sessionId 复制消息链，不污染原文件，cwd 落点正确（可安全用于测试）

**对设计的决定性影响**：
1. **「逻辑会话 = (cwd, title) 聚合」模型证伪**。每个 jsonl 文件 = 一个独立可 resume 的会话
2. **消息数过滤按单文件计**（更符合用户心智：「一个会话」= `/resume` 列表里的一个 sessionId），
   不再聚合求和。「在线设计文件」不是 222 条的会话，而是 19 个独立会话（最大 42 条）
3. **会话项的 resume 目标**必须取组合内**最新有实质内容的文件**——直接取链尾会 resume 进
   `/clear` 空壳空会话（严重 bug）。判据：有 ≥1 条真实用户消息（非 isMeta、非 command caveat
   包装、非 slash command）的最新文件
4. ConfigBackup 目录复核：会话是在备份目录里**真实运行过**的（cwd 指向备份区路径），
   非复制副本——但维持排除决策（用户不希望备份目录出现在任务列表）

**保底自动项的实际规模**：按单文件口径，13 个无手动命名项的目录里，12 个全部会话 <10 条、
1 个（写日志）只有无标题会话（不算候选，决策 37）→ **当前数据下保底自动项为 0 个**。
规则保留作未来数据变化的兜底。列表最终规模 ≈ 31 文件夹 + 8 手动会话项 ≈ **39 项**。

## 已确认的需求决策（用户 2026-08-27 回答）

| # | 决策 | 说明 |
|---|---|---|
| 1 | **只做终端命令**，不做会话内 `/xx` | 用户理解并接受「会话内无法切 cwd」的限制，放弃 `/xx` 入口 |
| 2 | **数据源 = 只读 CC 会话历史**，砍掉磁盘扫描 CLAUDE.md | 好处：没有项目级 CLAUDE.md 的文件夹也能被发现；零配置。代价：全新任务首次仍需手动 cd 一次 |
| 3 | **嵌套子任务全部平铺展示** | 父任务和子任务是列表里平等的独立项，靠活动排序和路径尾部区分，不做树形折叠 |
| 4 | **默认命令 = `claude --dangerously-skip-permissions -c`**（即 `claude-dc`），可配置修改 | |
| 5 | **命令名 = `cct`** | 已确认未被占用 |
| 6 | **安装方式 = 打印代码用户自己粘贴到 profile** | 先跑通功能，不自动改 profile |
| 7 | **界面 = 全屏实时过滤，单列纵向**（类似 Plugin Marketplace） | ↑↓ 移动 + 边打字边过滤 + 回车选定 + Esc 取消 |
| 8 | **同名会话只留最新一条** | 按 (cwd, title) 去重。`管理_sub2api` → 4 项（文件夹 + 账号调优 + codex伪装 + 签到）；`编写在线设计文档` → 1 项 |
| 9 | **目录不存在则不显示** | 每次列表时 Test-Path 过滤失效路径 |
| 10 | **文件夹项用 `-c`，具体会话项用 `--resume <sid>`** | 两种项类型执行不同命令 |
| 11 | **数据提取 = 读每文件前 50 + 后 50 行**，不做缓存 | 实测 100% 命中所需字段，无状态实现，1 秒内 |
| 12 | **排除 `agent-` 前缀文件、`journal.jsonl`、`.claude/worktrees/` 路径** | agent 文件占 52%，worktree 是工具临时区不是任务 |
| 13 | **排序用最后一条 timestamp，不用文件 mtime** | 5 个被复制过的备份文件 mtime 偏差 66-187 小时 |
| 14 | **cwd 取文件内最后一条的值** | 13 个文件会话中途 cwd 变了（`/add-dir`、中途 cd） |
| 15 | **会话项和文件夹项按文件夹聚合排序** | 文件夹之间按最近活动排；会话项紧跟自己的文件夹（内部也按时间排）。搜索命中会话项时把父文件夹一起显示作上下文 |
| 16 | **搜索匹配「文件夹名 + 会话标题 + 全路径」并集**，子串匹配 | 不做 fzf 式跳字符模糊匹配，不做拼音首字母匹配（需内置 20KB 汉字转拼音表，列表仅 70 项不值得） |
| 17 | **时间用相对格式** | `刚刚` / `3小时前` / `2天前`，超过 7 天转绝对日期 `08-05` |
| 18 | **路径只显示尾部 2 级，不显示全路径** | 但搜索仍匹配全路径 |
| 19 | **尾部级数动态调整**：同名冲突时自动多显示一级直到能区分 | 只影响冲突的那几项，其他项保持简短。实例：`源代码仓库/sub2api` vs `源代码理解/sub2api` 需要 3 级 |
| 20 | **自动标题（ai-title）作为文件夹行的副标题**，不单独占行 | 文件夹行右边显示「最近一次在干什么」。这解决了「保留一个但不与文件夹项重复」的问题 |
| 21 | **只有手动命名（custom-title）的会话才作为独立缩进项** | 列表 = 约 60 个文件夹（带副标题）+ 13 个手动命名会话 ≈ 70 项，无重复 |
| 22 | **退出 claude 后终端留在任务目录** | 符合 cd 直觉，可接着干活。想回原处用 `cd -` / `Pop-Location` |
| 23 | **`--resume` 失败时打印提示再降级为 `-c`** | 静默降级会让用户困惑为何进了别的会话 |
| 24 | **支持 `cct <关键词>` 作为初始过滤词**，唯一命中也不自动启动 | 熟悉后 `cct sub` + 回车两步到位；英文参数还能绕过中文 IME 无法实时过滤的问题 |
| 25 | **配置文件 = `~/.cct/config.json`**，首次运行自动生成带默认值 | 不放项目目录（个人配置不跟代码走）；JSON 而非 psd1（通用性好，便于手改或换语言重写） |
| 26 | **目录不存在时报错并留在原地**，不启动 claude | |
| 27 | **`claude` 启动失败直接透出错误** | |
| 28 | **空列表直接打印提示退出**，不进交互界面 | |
| 29 | **只有一项时不跳过界面**，仍需回车确认 | 避免「只是想看看有什么任务」时意外启动 |
| 30 | **文件夹副标题 = 该目录下最近会话的标题**，不管自动还是手动命名 | 保证「显示的副标题」与「回车执行 `-c` 实际接的会话」完全一致。副标题可能与下方缩进项同名（当最近会话正是手动命名那个），接受这个视觉冗余 |
| 31 | **列表用可滚动窗口** | 只渲染窗口能装下的行数，方向键移到边缘时自动滚动，右上角显示 `1-12/70` 计数 |
| 32 | **会话消息数阈值过滤**：**单文件（= 独立会话）≥10 条用户消息才保留** | 用户原话「对话次数小于10没必要显示」。**口径修正**（发现 22 二次探针）：同标题多文件是独立会话（非片段），按单文件计不聚合求和——原聚合模型基于「续接拆分」假设，已被证伪。实测早停方案：单文件数满 10 即停读，真实口径全库 3.18 秒 |
| 33 | **文件夹项永不消失**：每个存在目录至少保留一个快速入口 | 用户调整「一个目录下至少要保留一个会话可以快速进入」。目录下所有会话均 <10 条时，保留最近一个 |
| 34 | **手动命名的会话同样受 ≥10 条阈值约束**（2026-08-27 用户修订：「小于10次的会话，哪怕手动命名的，意义也不大」） | 修订前为「全保留」。现规则：组内 ≥阈值的文件里选最新的作 resume 目标；整组 <阈值 → 不保留（无防御回退）。实际效果：账号调优等项从 1 条碎片文件改指向 113 条的老文件 |
| 35 | **自动命名会话在 >10 条的里面每个目录保留最新一个** | 用户调整「自动命名的保留大于10次的里面保留一个」。此自动命名项作为该目录下唯一的快速入口候选，与文件夹项共存（当前数据下实际为 0 个，规则作未来兜底） |
| 36 | **自动命名项仅作保底**：目录已有手动命名项时不再显示自动命名项 | grilling 复审轮确认。理由：自动项与文件夹 `-c` 语义重叠（`-c` 接的就是最新会话），已有手动项时再加自动项是冗余。sub2api 保持 3 个缩进项 |
| 37 | **无标题会话不算「保留一个」的候选** | grilling 复审轮确认。无标题没有可显示的名字，显示「(未命名)」无信息量；全库只影响写日志 1 个目录，其文件夹项已够用 |
| 38 | **副标题在最近会话无标题时留空** | grilling 复审轮确认。实测 14/31 目录最新会话无标题。一致性优先（决策 30 精神：副标题 = `-c` 实际接的会话），宁缺勿假 |
| 39 | **搜索行右侧计数**：未过滤显示「共 N 项」，过滤后显示「M/N」（命中数/总数） | 用户 2026-08-27 补充。输入 cct 进入后搜索框最右侧显示总数，过滤后显示过滤后总数 |
| 40 | **搜索框空时显示淡色占位提示**「输入关键词，按Enter过滤」，居左 | 用户 2026-08-27 补充。有输入后占位提示消失，显示实际输入内容 |

> 注：决策 8 已被决策 20/21 取代（当时误以为 ai-title 与 custom-title 混在一起产生重复，
> 实测发现每个 (cwd, title) 组合本来就唯一，真正的问题是 ai-title 质量低而非重复）。
> 决策 21 中「会话项只收 custom-title」细化后受决策 33/34/35 调整：手动命名全保留，
> 自动命名每目录只保留 >10 条的最新一个，文件夹项永不因消息数过滤消失。

**最终列表形态（已确认）**：

```
搜索>                                      1-12/70

❯ 管理_sub2api    账号3244请求头覆写   1小时前
    └ 账号调优                      2天前
    └ 签到                          2天前
    └ codex伪装                     3天前
  PersonalAITaskCenter  git status概览  6天前
  写日志            日志模板整理        昨天
  ↓ 还有 58 项

  ↑↓ 选择   回车 启动   Esc 取消
```

## 需求盘问结论（mattpocock-skills:grilling，2026-08-27）

设计树已走完，**前沿为空**——所有分支均已探明，无静默假设。

盘问过程中纠正的 4 个错误理解：

1. **「17 条同名重复」是误判**：实测每个 (cwd, title) 组合本来就唯一，
   真正的问题是 ai-title 质量低（`git status概览`、`Review pasted text`），不是重复。
   → 改为「ai-title 作副标题，只有 custom-title 作独立项」
2. **「标题为主的展示」不可行**：322/445 个会话文件（72%）根本没有标题。
   → 显示名必须以目录名为主
3. **「用 mtime 排序提速」不可行**：ConfigBackup 里 5 个被复制过的文件 mtime 偏差 66-187 小时。
   → 必须解析 timestamp
4. **「需要缓存」是多余的**：读前 50 + 后 50 行即 100% 命中所需字段，1 秒内完成。
   → 无状态实现

盘问中新发现的硬约束：

- **中文 IME 无法逐键实时过滤**（终端机制决定，非能力问题）→ 靠 `cct <英文关键词>` 参数缓解
- **`--resume` 不会自动回到会话原目录**，必须先 `Set-Location` 再 resume，顺序不能颠倒
- **`cwd` 会话中途会变**（13 个文件），必须取最后一条

## 待办：需求定稿后的验证步骤

- [x] 用 `mattpocock-skills:grilling` 盘问需求，确认没有说不清的地方（**用户明确要求**）— 已完成，前沿为空
- [x] grilling 复审轮（用户 2026-08-27 再次要求）：决策 36/37/38 落定；唯一悬置依赖「多文件会话 resume 历史完整性」探针进行中，其余前沿为空

## 用户原始需求（原文保留）

### 背景
我在电脑上有许多文件夹级别的任务，里面都可能包含了 Claude.md 文件。每次我会在终端里用 cd 命令定位过去，
然后启动 claude code。但是现在我经常需要同时启动多个任务，有的任务是长任务，有的很短，
经常要去复制地址再到终端里去 cd，这很烦。

### 大体想法
1. 搞一个 claude code 的插件或者一个独立的命令行程序，不是 skill。
2. 在终端进入 claude code 之后可以通过「/xx」的指令，下方出现一些可以选择的界面（类似 `/plugin`），
   直接选择我本地这些任务文件夹
   - 2.1 在终端中直接输入 xxx 命令出现界面进行选择，选择后自动 cd 到对应路径下再直接执行启动 claude code 的命令
     （执行的命令需要可配置）
   - 2.2 以上两种使用方式二选一
3. 这个工具/插件安装完之后，可以手动触发扫描（扫描包含项目级 claude.md 的文件夹），扫描任务里执行的是「添加or更新」
   - 3.1 [可选]进入 claude code 会话时，会向工具的数据里注册一下，「添加or更新」一下数据。
     这样就避免要经常调用上面的命令去扫描更新
4. 显示方面，考虑终端里显示就可以了，目前主要用的都是「windows 终端」，在 powershell 环境下运行的。
   - 4.1 每一项的显示，我想的是至少要包含「直接文件夹的名字」，右边会有最近 2 级或者 3 级的名字，
     不要从根路径一显示过来。然后有可能的话，每一项的右边，可以用淡一点颜色显示全路径。
     我这里有些任务里面还有子任务，子任务也是有项目级 Claude.md 的。
5. 排序方面，我想的是最近的活动显示在最上面

## 环境约束

- Windows 11 Pro 26200
- Windows 终端 + PowerShell 7（pwsh）
- Claude Code 版本 2.1.237
- 大量中文路径（编码/显示宽度是坑）
